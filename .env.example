# Used by docker-compose.yml. Do not commit a real password.
ADMIN_PASSWORD=replace-with-a-strong-demo-password
# Public token only; restrict it to local and deployed frontend origins.
VITE_MAPBOX_ACCESS_TOKEN=pk.replace-with-your-public-mapbox-token
