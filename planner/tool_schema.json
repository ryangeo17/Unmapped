{
  "note": "Function declarations for the campus walking route planner. The shape is the OpenAPI subset Gemini, Claude and OpenAI all accept; adapt the wrapper key per provider.",
  "functionDeclarations": [
    {
      "name": "plan_route",
      "description": "Plan a walking route across the JHU Homewood campus. Returns real geometry from A* over the campus path network, with distance, time and a leg breakdown. Always call this rather than describing a route from memory; never write or adjust coordinates yourself. This plans walking routes only and knows nothing about wheelchair access.",
      "parameters": {
        "type": "object",
        "properties": {
          "origin": {
            "type": "string",
            "description": "Where the trip starts, as a building or outdoor space name, e.g. 'Malone Hall'. Free text is resolved against the place index."
          },
          "destination": {
            "type": "string",
            "description": "Where the trip ends, e.g. 'San Martin Garage'."
          },
          "allow_shortcuts": {
            "type": "boolean",
            "description": "Default true. Lets the route cut straight across open lawns where that is faster. Set false to stay on the official paved network, for instance in rain or at night, or to show the user what the shortcut saves."
          }
        },
        "required": ["origin", "destination"]
      }
    },
    {
      "name": "list_places",
      "description": "Every routable place name on campus, with its use and any alias. Call this when a name the user gave does not resolve, or when plan_route returns status 'ambiguous'.",
      "parameters": { "type": "object", "properties": {} }
    }
  ]
}
